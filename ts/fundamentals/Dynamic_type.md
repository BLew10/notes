# Dynamic Type  - `any`
- `any` can be any type 
```ts
let age: any = 25;
age = true;
age = 'string';
let mixed: any[] = [];
//mixed can now hold all data types
```
- Think of `any` like variables in js

